{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build raft_test",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && cmake .. && make -j4",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}